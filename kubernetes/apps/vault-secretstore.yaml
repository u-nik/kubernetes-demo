apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-secretstore
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://bitbucket.org/storelogix/kubernetes-demo.git
    targetRevision: main
    path: kubernetes/manifests/vault-secretstore
    helm:
      releaseName: vault-secretstore
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
